<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <queries>
        <intent>
            <action android:name="android.intent.action.DIAL" />

            <data android:scheme="tel" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />

            <data android:scheme="geo" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />

            <data android:scheme="https" />
        </intent>

        <package android:name="com.whatsapp" />
        <package android:name="com.google.android.apps.maps" />
    </queries>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/always_hungry_icon_squircle"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/always_hungry_icon_squircle"
        android:supportsRtl="true"
        android:theme="@style/Theme.AlwaysHungry">
        <activity
            android:name=".DessertsActivity"
            android:exported="false"
            android:label="Desserts"
            android:parentActivityName=".MainActivity"/>
        <activity
            android:name=".StartersActivity"
            android:exported="false"
            android:label="Starters"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".MainCoursesActivity"
            android:exported="false"
            android:label="Main Courses"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>